local function plate()
  return {
    idname = 'IC2:itemPartCarbonPlate',
    data = 0,
  }
end

local function crystal()
  return {
    idname = 'IC2:itemBatCrystal',
    data = nil,
  }
end

return {
  shape = {
    plate(), crystal(), plate(),
    plate(), nil, plate(),
    plate(), nil, plate(),
  },
  output = {
    item = {
      idname = 'IC2:itemArmorNanoLegs',
      data = 0,
    },
    count = 1,
  },
  type = 'craft',
}

-- vim: ft=lua shiftwidth=2 expandtab colorcolumn=45 tw=44
